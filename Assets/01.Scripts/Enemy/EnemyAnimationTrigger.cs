using BGD.Agents;
using UnityEngine;

namespace BGD.Enemys
{
    public class EnemyAnimationTrigger : AgentAnimationTrigger
    {
        public override void Initialize(Agents.Agent agent)
        {
            base.Initialize(agent);
        }
    }
}
